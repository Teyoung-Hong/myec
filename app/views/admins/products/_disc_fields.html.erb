<%= f.text_field :disc_name %>
<div class="newsted-field">
<%= link_to_remove_association 'Remove disc', f %>
</div>
<%= f.fields_for :songs do |song| %>
  <%= render 'song_fields', f: song %>
<% end %>
<div class="links">
<%= link_to_add_association 'Add song', f, :songs %>
</div>

